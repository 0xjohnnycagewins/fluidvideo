import { Web3Provider } from '@ethersproject/providers';
import { Framework } from '@superfluid-finance/js-sdk/src/Framework';
import React, { useEffect } from 'react';

interface Props {}

export const Superfluid: React.FunctionComponent<Props> = () => {
  return <>{}</>;
};
